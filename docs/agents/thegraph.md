# thegraph — what this repo is built against

## What this project is

Shared React UI and client data-layer packages (`@just-apps/auth`, `@just-apps/subscription`), published to npm for the Just Apps family of apps.

## References

| Source | Informs | Reached by | Binding |
|---|---|---|---|
| Consumer apps — the sibling repos depending on `@just-apps/*` (`grep -l '"@just-apps/' ../*/package.json ../*/*/package.json`) | how it works | their source trees — raw | binding |
| Lemon Squeezy (Lemon.js overlay, checkout) | how it works | the published docs — **summarized** | binding |
| supabase-js v2 | how it works | its source tree — raw | binding |
| shadcn/ui | how it works | its source tree — raw | example |
